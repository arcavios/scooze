#  0 7 * * 1

# TODO: before I can implement this workflow I need to be able to get logs
# Add logging to our CLI commands

# TODO: pseudocode
# pip install scooze
# scooze load cards --all
# check the size of the log, maybe report some of the stuff?

name: Scryfall Bulkdata Validation

on:
    [push]
    # Every Monday at 07:00 UTC
    # schedule:
    #     - cron: "0 7 * * 1"

jobs:
    setup-matrix:
        runs-on: ubuntu-latest
        outputs:
            matrix: ${{ steps.setup-matrix.outputs.matrix }}
        steps:
            - id: setup-matrix
              uses: druzsan/setup-matrix@v2
              with:
                  # Use | to preserve valid YAML syntax
                  matrix: |
                      os: ['ubuntu-latest'] # 'macos-latest'
                      python-version: ['3.11', '3.12']
    setup-python:
        runs-on: ubuntu-latest
        steps:
            #----------------------------------------------
            #       check-out repo and set-up python
            #----------------------------------------------
            - uses: actions/checkout@v4
            - uses: actions/setup-python@v5
            #----------------------------------------------
            #        load pip cache if cache exists
            #----------------------------------------------
            - uses: actions/cache@v4
              with:
                  path: ~/.cache/pip
                  key: ${{ runner.os }}-pip
                  restore-keys: ${{ runner.os }}-pip
    do-a-thing:
        needs:
            - setup-matrix
            - setup-python
        runs-on: ubuntu-latest
        steps:
            - run: |
                  docker info
                  docker ps -a
